- defaults:
    link:
        create: true
        relink: true

- clean: ['~']

- link:
    ~/.gitconfig:
    ~/.vim:
    ~/.vimrc:

- shell:
    - [git submodule update --init --recursive, Installing submodules]
